import { StyleSheet } from 'react-native';

const styles: any = StyleSheet.create({
    submitButton: {
        width: '100%',
        marginTop: 20
    },
    slider: {
        width: '50%',
    }
});
export default styles;
